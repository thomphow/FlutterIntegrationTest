name: example
description: example
publish_to: none
version: 0.0.0

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  # intl: ^0.17.0
  # firebase_core: ^1.1.0
  # firebase_auth: ^3.0.1
  # cached_network_image: ^3.0.0
  # google_sign_in: ^5.0.2
  # flutter_spinkit: ^5.0.0 # For Show Progressbar
  # local_auth: ^1.1.7
  flutter:
    sdk: flutter

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  yaml: ^3.1.0

flutter:
  uses-material-design: true

